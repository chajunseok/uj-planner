# U.J planner — 디자인 시안 구현 스펙

Claude Design 프로젝트 `UJ planner - 화면.dc.html`(방향 1a)에서 구현에 필요한 값만 뽑은 문서.
원본: https://claude.ai/design/p/cdc60b48-1c00-4491-920c-ce60adcc7c66
위임 프롬프트는 `260918.uj-planner.design.md`.

라이트 단일 테마. 모든 수치는 dp/sp. 시안의 0-4 「플라워」 색 조정안(로즈 primary·세리프 제목)은
**승인 대기 제안**이라 반영하지 않음 — 색은 `ui/theme/` 한 곳에 있으므로 승인되면 거기만 교체.

## 색 (Material 3 ColorScheme, 라이트)

| role | hex | role | hex |
|---|---|---|---|
| primary | `#3F5E8F` | onPrimary | `#FFFFFF` |
| primaryContainer | `#D6E3FF` | onPrimaryContainer | `#0F2A4E` |
| tertiaryContainer | `#F6E3B3` | onTertiaryContainer | `#4A3300` |
| surface | `#FBF9F6` | onSurface | `#1C1B19` |
| surfaceVariant | `#E6E2DD` | onSurfaceVariant | `#484643` |
| surfaceContainerLow | `#F6F4F0` | surfaceContainer | `#F3F0EB` |
| surfaceContainerHigh | `#EDEAE4` | surfaceContainerHighest | `#E6E2DD` |
| outline | `#7A7770` | outlineVariant | `#CBC7C0` |
| error | `#B3261E` | errorContainer / on | `#F9DEDC` / `#410E0B` |
| scrim | `#1C1B19` @ 42% | 앱 아이콘 배경 | `#FBF7F2` |

보조 회색: 주말 요일·버림 글자 `#8E8C87`, 평일 요일 라벨 `#6B6A66`, 비선택 칩 테두리·버림 테두리 `#BDB9B2`.
"끝!" 카드: `#E8F1E9` / `#0D3F1B`.

## 일정 색 8쌍 (bg / fg, 모두 AA)

초록 `#CDEBD1`/`#0D3F1B` · 파랑 `#D3E4FA`/`#0B3560` · 호박 `#F6E3B3`/`#4A3300` · 보라 `#E6DBF7`/`#37215F` ·
청록 `#C6ECE9`/`#003F3B` · 장미 `#F9D9DC`/`#5A1420` · 라임 `#E4EDBA`/`#2F3F00` · 귤 `#FBDCC6`/`#5C2600`

## 타이포 (시스템 폰트, 숫자는 `tnum`)

headlineMedium 34/600 커버 제목 · headlineSmall 30/600 플렉스 카드 제목 · titleLarge 22/600 · titleMedium 20/600 앱바 ·
titleSmall 16/600 · bodyLarge 16/400 · bodyMedium 14/400 · bodySmall 12/400 · labelLarge 14/600 버튼 ·
labelMedium 13/600 칩·배너 · labelSmall 11/700 블록 약어·요일 헤더 · blockSub 9/500 · axis 10/500

## 치수

| 항목 | 값 |
|---|---|
| 그리드 30분 행 | 22 (08–24시 16시간 = 704, 스크롤 없음) |
| 시간축 폭 / 우측 여백 | 32 / 8 → 열 폭 (411−32−8)/7 = 53 |
| 블록 여백·반경·패딩 | 좌우 2·상하 1 / 6 / 상하 3·좌우 4 |
| 카드 반경 | 목록 16 · 커버·플렉스 20 · 시트·다이얼로그 28 |
| 버튼 높이 | 기본 48 · 저장 56 · 커버 완료 64 |
| 칩 높이 | 40 (터치 타겟 48) |
| 화면 좌우 여백 / 하단 바 | 20 / 72 |
| 힌지 회피 | ±16 (y 464–496) |
| 커버 안전 여백 | 전체 28 · 모서리 40×40 |

## 50dp 열 밀도 해법

이름 대신 **색 + 앞 2글자**(공백 제거). 고정 일정은 1시간 이상이면 전체 이름. 1행 = 약어 + 12dp 상태 아이콘,
2행 = 9sp 시작 시각(높이 ≥ 44dp일 때만). 탭하면 상세 카드. 세로 쓰기·3일↔7일 전환은 채택 안 함.

## 블록 상태

| 상태 | 표현 |
|---|---|
| 고정 | surfaceVariant / onSurfaceVariant, 아이콘 없음 |
| 예정 | 팔레트 bg / fg |
| 진행 중 | + 2dp surface 링 + 2dp fg 링 |
| 완료 | alpha 0.6 · check_circle · 취소선 |
| 못함 | bg = surface · 2dp 점선 fg · replay |
| 버림 | 투명 · 1dp `#BDB9B2` · close · 취소선 · 글자 `#8E8C87` |
| 드래그 중 | scale 1.08 · 그림자 · 2dp 실선 · open_with. 원래 자리는 1dp 점선 윤곽, 놓을 수 있는 빈칸은 primaryContainer 표시, 30분 스냅 |

## 화면

- **A1 주간 그리드**: 상단 "9월 셋째 주" + "9.14 – 9.20 · 이번 주" + 설정. 배치 실패 배너(44dp, tertiaryContainer, 실패 0이면 안 그림) →
  탭하면 일정별 못 넣은 횟수·조건 요약·`조건 바꾸기`/`다시 짜기`. 오늘 열 강조(날짜 원 primary), 현재 시각선 error. 하단 바: ‹ 이번 주 › + `일정` FAB.
- **A2 이동**: 길게 눌러 드래그. 상단 안내 "운동 · 놓을 자리로 끌어 주세요 / 금 19:00 – 20:00". 하단 `취소` / `이번 주 버림`.
- **A3 빈 상태**: "먼저 움직이지 않는 일정을 넣어 주세요" + `고정 일정 추가` / `가변 일정부터 넣기`.
- **A4 고정 등록**: 이름 · 요일 다중 선택 · 시작 시각(±30분) · 소요시간 칩(30분·1·1.5·2·3시간·직접) · 요약 줄 · 저장 56dp · 삭제.
- **A5 가변 등록**: 이름 · 색 스와치 4×2 · 소요시간 칩 · 주당 횟수 스테퍼 · 우선순위 3칸 · 선호 시간대 4칸(아이콘+글자) · 마감 요일 칩(없음+월~일) ·
  예상 배치 한 줄 · `저장하고 배치`.
- **A6 밀린 일정 시트**(진입 시 자동): "지난 일정 N개, 어떻게 됐어요?" 항목당 했음(filled)·못함(tonal)·버림(outlined), 비율 1.3:1:1, 48dp.
  처리된 항목은 결과 줄("→ 금 08:00으로 옮겼어요" + 되돌리기)로 상단에 쌓임. 하단 `남은 N개 모두 했음` / `나중에`. 오래된 것부터.
- **A7**: 못함인데 빈칸 없음 → 다이얼로그 "이번 주엔 빈칸이 없어요" `이번 주는 버림` / `다시 짜기`. 재배치 성공 시 스낵바 + 되돌리기.
- **A8 설정**: 요일별 시작–종료(자정 넘으면 +1 표시), `월 → 평일 전체`·`토 → 주말` 복사, `이번 주 다시 짜기`, 버전 줄.
- **B 플렉스**: 위 = 요일·날짜·남은 개수 + 오늘 타임라인, 아래 = 지금 카드(kicker·제목·다음) + 완료 56dp / 못함. 밀린 일정 있으면 그 카드 우선, 다 끝나면 "끝!" + `이번 주 보기`.
- **C 커버**: 상단 시각 / 오늘 남은 개수, 중앙 색 점 + kicker 14 + 제목 34 + 부제 16, 하단 완료 64dp + 못함 텍스트 버튼. 지금 없으면 "다음" 카드 + `미리 완료`, 다 끝나면 "끝!".
- **0 아이콘·스플래시**: 적응형 아이콘 — 배경 `#FBF7F2`, 전경 튤립 아트워크 70dp, 단색은 튤립 실루엣 벡터. 스플래시 배경 surface, 별도 애니메이션 없음.
  로딩 전용 화면 없음(계산 100ms 이내).

## 시안과 다르게 가는 것

| 시안 | 구현 | 이유 |
|---|---|---|
| 튤립 전경 1024px 원본에서 5개 밀도 내보내기 | 사용자가 내보낸 432px(xxxhdpi) 한 장만 사용 | 원본 PNG가 가져오기 도구 상한(256KiB)을 넘어 잘림. 432px 이면 가장 높은 밀도라 시스템 축소로 충분 |
| 블록 이동 중 하단 바 `취소` / `이번 주 버림` | 놓을 수 없는 곳에 놓으면 취소, `버림`은 블록 상세 카드에 | 끄는 손가락으로는 하단 버튼을 누를 수 없음 |
| 재계산 중 스켈레톤·진행 바 | 생략 | 시안도 "300ms 넘을 때만 보임"이라 명시. 실측해서 느리면 추가 |
| 스플래시 AnimatedVectorDrawable | 생략 | 시안에서 "선택" |
